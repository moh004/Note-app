<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="styles.css">
    <title>Document</title>
</head>
<body>
    <div class="header"><%- include("__buttons") %></div>
    <div class="wrapper">
        
        <div class="line-num">
            <% for (let i = 1; i <= code.split('\n').length; i++){ %>
                <p><%= i %></p>
                <% } %> 
        </div>
        <pre><code id="code-text"><%= locals.code %>
            </code></pre>

    </div>
   
</body>
</html>